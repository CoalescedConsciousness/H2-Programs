using System.Xml.Serialization;
using System.IO;

namespace Common
{
    internal class XMLHelper
    {
        public static void ReadFile(Type type, string path)
        {
            XmlSerializer write = new XmlSerializer(typeof(object));

        }
    }
}
